<!--
适用阶段：重构与演进期（新旧架构/技术栈并存，处于迁移过程中）
使用说明同上；本阶段的核心矛盾是"新旧并存"，
所有临时性约定都必须标注失效条件，避免迁移完成后规则变成新的历史包袱。
-->

# 项目记忆文件 · 重构与演进期

## 项目定位
- 项目简介：[一句话]
- 重构目标：[如"从 REST 迁移到 GraphQL"，"从单体拆分为微服务"，说明动机]
- agent 的范围边界：迁移期间新旧代码并存，agent 必须先判断当前改动
  属于"旧架构维护"还是"新架构建设"，不确定时向用户确认。

## 技术栈与架构（新旧并存说明）
- 旧架构：[技术栈 + 目录范围，如 `src/legacy/`]
- 新架构：[技术栈 + 目录范围，如 `src/v2/`]
- 迁移边界判断规则：[如何判断一处改动该用旧方式还是新方式，
  例："`src/legacy/` 下只做最小必要修复，不新增功能；
  新功能一律在 `src/v2/` 下用新架构实现"]
- 当前迁移进度：[如"用户模块已迁移，订单模块未迁移"，只写事实，不写百分比估算]

## 常用命令
- 旧架构构建/测试：`[命令]`
- 新架构构建/测试：`[命令]`
- 迁移校验脚本（如有）：`[命令]`

## 代码规范
- 旧架构：[维持原有规范不变，仅做必要修复]
- 新架构：[采用的新规范，具体、可验证]
- 禁止在旧架构中引入新架构的模式，避免出现"三套风格"并存

## 禁止事项 / 高危操作
- 不要在未完成迁移的模块中同时修改新旧两套实现。
- 不要删除旧架构代码，除非确认对应功能已在新架构中验证通过。
- [其他项目特有的红线]

## 测试与验证流程
- 新旧行为一致性校验方式：[如双写对比 / 灰度对比测试]
- "迁移完成"的判定标准：[明确的可验证条件，而非主观判断]

## 临时性约定（本阶段特有，需标注失效条件）
- [约定内容] —— 失效条件：[如"订单模块迁移完成后即可删除此约定"]

## 外部文档引用
- 迁移方案设计文档：[链接]
- 新旧架构对照表（如有）：[链接]

## 待确认事项
- [ ] 迁移完成后，本文件应合并回"迭代与业务演进期"版本，是否需要提前规划合并时间点？
- [ ] 当前是否有模块的迁移边界判断规则仍不清晰，需要与团队进一步确认？