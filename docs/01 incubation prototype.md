<!--
适用阶段：孵化与原型期（PoC / MVP 早期，架构和技术选型可能随时推翻重来）
使用说明：
  - 本文件是「通用版本」，不绑定具体工具语法；正式生效时通过 @import 或软链接
    映射为根目录的 CLAUDE.md / AGENTS.md / GEMINI.md。
  - 方括号 [ ] 内为待填内容；如暂时无法确认，保留 [TODO: 待确认] 标记，不要臆造。
  - 本阶段原则：宁可少写，不要写死。规范一旦写下就会被 agent 当真，
    过早固化的规则比缺失的规则更危险。
-->

# 项目记忆文件 · 孵化与原型期

## 项目定位
- 这是什么：[一句话说明项目要验证的核心假设/要解决的问题]
- 给谁用：[目标用户或使用场景，如"仅供内部验证，非生产环境"]
- agent 的范围边界：这是一个原型/PoC，**不追求生产级质量**，目标是快速验证可行性，
  允许临时代码、硬编码配置、跳过边界情况处理。

## 技术栈与架构
- 技术选型（当前，可能变化）：[语言/框架/关键依赖 + 版本，如 "Next.js 15 + TypeScript"]
- 说明：技术选型可能会因为验证结果而更换，**不要基于此写死复杂的架构假设**。
- 目录结构：[仅列出当前明确的顶层目录，不需要穷举]

## 常用命令
<!-- 按 GitHub 对 2500+ 仓库的分析，命令应尽早给出、带上关键参数，而不仅是工具名 -->
- 安装依赖：`[命令]`
- 启动本地环境：`[命令]`
- 运行测试（如有）：`[命令，若原型期暂无测试可留空并说明]`

## 代码规范
- [仅写 linter/formatter 无法自动强制的约定，例如目录命名习惯]
- 本阶段不强制统一风格，允许探索性代码，但**禁止提交明文密钥/密码**。

## 禁止事项 / 高危操作
- 不要连接生产数据库或生产环境的第三方服务。
- 不要提交 `.env`、密钥、真实用户数据样本。
- [其他明确禁止的操作]

## 测试与验证流程
- 本阶段"改完了"的标准：[如"手动跑通核心流程即可，暂不要求自动化测试覆盖率"]

## 外部文档引用
- [如有产品需求文档/设计稿链接，在此引用，不整段粘贴内容]

## 待确认事项（本阶段特有）
- [ ] 技术选型是否已经基本稳定，可以进入"迭代与业务演进期"？
- [ ] 是否已有需要长期遵守的硬性约束（如合规要求）需要提前写入？