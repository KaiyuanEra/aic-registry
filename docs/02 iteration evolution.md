<!--
适用阶段：迭代与业务演进期（架构已定型，团队协作频繁，功能迭代速度最快）
使用说明同上；本阶段是文件最容易膨胀的阶段，注意：
  - 只写 linter 管不到的规则
  - 细节内容拆到外部文档，用路径/链接引用
  - 建议控制在 200 行以内，接近上限时优先拆分而不是继续堆砌
  - 单体仓库（monorepo）可在各子包目录下放置独立的记忆文件，
    agent 会自动读取离当前工作目录最近的一份，就近优先。
-->

# 项目记忆文件 · 迭代与业务演进期

## 项目定位
- 项目简介：[一句话]
- 使用场景/目标用户：[...]
- agent 的范围边界：[明确说明 agent 可以自主决定的范围，以及必须经人工确认的范围]

## 技术栈与架构
- 技术栈（含版本）：[如 "React 18 + TypeScript 5 + Vite + Tailwind CSS"，
  务必具体到版本，避免只写"React 项目"]
- 模块划分：[核心业务逻辑所在目录，用路径引用，不贴代码]
    - 例：`src/features/<feature>/` 每个业务功能独立目录
- 单体仓库说明（如适用）：[各子包路径 + 是否各自维护独立记忆文件]

## 常用命令
- 安装依赖：`[命令]`
- 本地开发：`[命令]`
- 构建：`[命令]`
- 测试（含关键参数/flag）：`[命令，如 pytest -v 而不仅是 pytest]`
- Lint / 格式化：`[命令]`

## 代码规范
<!-- 一个真实代码示例胜过三段文字描述；只写 linter 强制不到的部分 -->
- [具体、可验证的规则，例如："服务端组件默认不加 'use client'，
  只有确实需要浏览器 API 时才加"]
- 代码风格示例（可选）：
  ```
  [一小段能体现项目风格的真实代码片段]
  ```

## 禁止事项 / 高危操作
- **绝不提交密钥/凭证**（社区反馈中最常见的高价值约束）。
- 不要修改 [如：数据库迁移脚本目录 / 第三方计费对接代码] 除非明确要求。
- 不要跳过 CI 中定义的检查项。
- [其他项目特有的红线]

## 测试与验证流程
- "改完了"的标准：[如"新增/修改逻辑需有对应单测，CI 全绿后才可合并"]
- CI 配置位置：`[路径，如 .github/workflows/]`

## Git / 提交与协作规范
- 提交信息格式：[如 Conventional Commits：`feat: ...` / `fix: ...`]
- PR 规范：[分支命名、review 要求、合并策略]

## 外部文档引用
- 详细设计文档：[链接/路径]
- API 文档：[链接/路径]

## 待确认事项
- [ ] 是否存在多个子包，需要为每个子包单独建立记忆文件？
- [ ] 当前代码规范是否已被 linter 完全覆盖？若是，可从本文件精简掉重复条目。