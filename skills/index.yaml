# Code generated by aic-skills/scripts/indexgen. DO NOT EDIT.
registry_version: "v0.1.0"
skills:
    - name: "aic-contexts-creator"
      version: "1.5.0"
      description: "Create, migrate, and incrementally update development-stage project-level long-term memory context packages for the context registry, producing and validating contexts/<context-name>/CONTEXT.md and content.md, bumping versions and syncing contexts/index.yaml. Use when user requests writing or standardizing CLAUDE.md, AGENTS.md, Agents.md, project memory files, or project context, migrating old memory into a registry context, switching between incubation/iteration/maintenance/refactor stage guidelines, or creating projects similar to contexts/project-coding-guideline. Do NOT use for ordinary business documentation, one-off session prompts, skill authoring, or modifying tool implementations outside the context registry."
      tags:
        - "context-authoring"
        - "meta"
        - "project-memory"
      path: "skills/meta/aic-contexts-creator"
    - name: "aic-mcp-creator"
      version: "1.1.0"
      description: "Create, review, and incrementally update structured MCP server packages for the aic registry, generating and validating mcp-servers/<name>/MCP-SERVER.md. Handles stdio, SSE, and Streamable HTTP transports, environment variable placeholders, target client capabilities, version bumping, and mcp-servers/index.yaml. Use when user requests adding an MCP server definition, writing an MCP-SERVER.md, registering an MCP config into the registry, modifying an existing MCP source model, or checking MCP transport compatibility with Claude/Codex/Gemini/OpenCode. Do NOT use for implementing aic client parser/adapter, directly editing IDE native MCP config, starting an MCP server, or writing ordinary skills, contexts, or one-off MCP usage guides."
      tags:
        - "mcp"
        - "meta"
        - "registry-authoring"
      path: "skills/meta/aic-mcp-creator"
    - name: "aic-skill-creator"
      version: "2.2.0"
      description: "Author, design, and improve aic SKILL.md files. Use when creating a new skill from scratch, writing a SKILL.md, improving a skill description trigger quality, designing a skill directory structure, or when user mentions how to write a skill, description not triggering, which directory a skill belongs in, how to configure env-required, or how to add an adapter layer. Do NOT use for aic install/remove/sync commands, managing already-installed skills, or writing business code documentation unrelated to skills."
      tags:
        - "aic"
        - "meta"
        - "skill-authoring"
      path: "skills/meta/aic-skill-creator"
    - name: "brainstorming"
      version: "1.0.0"
      description: "You MUST use this before any creative work - creating features, building components, adding functionality, or modifying behavior. Explores user intent, requirements and design before implementation."
      tags:
        - "design"
        - "planning"
        - "requirements"
      path: "skills/superpowers/brainstorming"
    - name: "challenge"
      version: "1.0.1"
      description: "Review code written by others or AI from a complete stranger perspective, finding risk points that would actually cause production incidents, sorted by severity, without noise. Use when reviewing code written by others or AI, checking before merging MR, or when user mentions challenge, code review, review code, check for risks, is this code safe, pre-merge check, MR check, or help me review. Do NOT use for reviewing your own code (confirmation bias; needs physical sub-agent isolation), code style/naming/formatting suggestions, architecture discussion without concrete code, performance benchmarking without measured data, or auto-triggered scenarios."
      tags:
        - "code-review"
        - "dev-workflow"
        - "security"
      path: "skills/common/challenge"
    - name: "dev-plan"
      version: "2.0.3"
      description: "Generate or incrementally update the project development plan document dev-plan.md, breaking down Phase/Task structure, with Phase-level archiving and document length management. Use when writing a development plan, breaking down a PRD into tasks, updating an existing plan, archiving completed phases, or when user mentions create a development plan, break down tasks, update plan, project planning, requirement breakdown, technical approach, task list, Phase division, dev-plan, PRD, milestone planning, archive Phase, or simplify plan. Do NOT use for creating GitLab Issues or Milestones (use glab-manage skill), git commit operations (use git-commit skill), or pure technical implementation questions."
      tags:
        - "dev-workflow"
        - "docs"
        - "planning"
      path: "skills/common/dev-plan"
    - name: "dispatching-parallel-agents"
      version: "1.0.0"
      description: "Use when facing 2+ independent tasks that can be worked on without shared state or sequential dependencies"
      tags:
        - "dev-workflow"
        - "parallelization"
        - "subagents"
      path: "skills/superpowers/dispatching-parallel-agents"
    - name: "docker-deploy"
      version: "1.1.1"
      description: "Generate the full set of Docker local deployment files (Dockerfile, docker-compose.yml, build.sh, .dockerignore), output to the docker/ directory. Use when generating Docker deployment files, creating docker-compose, creating a build.sh script, initializing a docker directory, or when user mentions Dockerfile, docker-compose, container deployment, or build.sh. Do NOT use for K8s deployment config generation (use k8s-deploy), CI/CD pipeline config (use cicd-pipeline), or just discussing Docker concepts without needing to generate files."
      path: "skills/domain/docker-deploy"
    - name: "executing-plans"
      version: "1.0.0"
      description: "Use when you have a written implementation plan to execute in a separate session with review checkpoints"
      tags:
        - "dev-workflow"
        - "execution"
        - "planning"
      path: "skills/superpowers/executing-plans"
    - name: "finishing-a-development-branch"
      version: "1.0.0"
      description: "Use when implementation is complete, all tests pass, and you need to decide how to integrate the work"
      tags:
        - "dev-workflow"
        - "git"
        - "integration"
      path: "skills/superpowers/finishing-a-development-branch"
    - name: "git-commit"
      version: "1.0.4"
      description: "Generate standardized commit messages from git changes and development context, then execute the commit. Use when committing code, writing a commit message, or completing a development task, or when user mentions commit my code, write a commit message, stage and commit, code is done, task is complete, or git commit. Do NOT use for git push, merge, rebase, cherry-pick or other git operations, only viewing git status, or pulling remote code."
      tags:
        - "commit"
        - "dev-workflow"
        - "git"
      path: "skills/common/git-commit"
    - name: "glab-manage"
      version: "3.0.1"
      description: "Create GitLab issues from a development plan, link them to an existing milestone, and write issue numbers back to dev-plan.md. Use when creating GitLab issues, syncing a dev plan to GitLab, or when user mentions create issue, sync plan to GitLab, glab, GitLab issue, issue number writeback, sync tasks, create issues for this sprint, close issue. Do NOT use for local git commit (use git-commit skill), only viewing GitLab info without creating, push/pull operations, or creating milestones."
      tags:
        - "dev-workflow"
        - "gitlab"
        - "project-management"
      path: "skills/common/glab-manage"
    - name: "k8s-deploy"
      version: "1.1.1"
      description: "Generate K8s full deployment files (k8s.yaml, with ConfigMap+Secret+Deployment+Service+Ingress) and incremental update files (update.yaml, Deployment only), keeping the Deployment spec strictly consistent across both files. Use when generating K8s deployment files, creating k8s yaml, creating Deployment/Service/Ingress config, updating k8s image version, or generating incremental update files, or when user mentions K8s, Kubernetes, or kubectl. Do NOT use for Docker Compose deployment (use docker-deploy), CI/CD config (use cicd-pipeline), or Helm Chart generation."
      path: "skills/domain/k8s-deploy"
    - name: "readme-doc"
      version: "1.0.0"
      description: "Generate or update a professional README.md for a project, including badges, project overview, tech stack, architecture, project structure, deployment and usage, and license sections. Use when creating or rewriting a README, documenting a project for the first time, or when user mentions write README, generate docs, project intro, README.md, project description, add documentation, update README, write an introduction, project docs, or how to write a readme. Do NOT use for API documentation, code comments, CHANGELOG, dev-plan development plans, or only updating a single section rather than the whole README."
      tags:
        - "dev-workflow"
        - "docs"
        - "readme"
      path: "skills/common/readme-doc"
    - name: "receiving-code-review"
      version: "1.0.0"
      description: "Use when receiving code review feedback, before implementing suggestions, especially if feedback seems unclear or technically questionable - requires technical rigor and verification, not performative agreement or blind implementation"
      tags:
        - "code-review"
        - "dev-workflow"
        - "feedback"
      path: "skills/superpowers/receiving-code-review"
    - name: "requesting-code-review"
      version: "1.0.0"
      description: "Use when completing tasks, implementing major features, or before merging to verify work meets requirements"
      tags:
        - "code-review"
        - "dev-workflow"
        - "verification"
      path: "skills/superpowers/requesting-code-review"
    - name: "subagent-driven-development"
      version: "1.0.0"
      description: "Use when executing implementation plans with independent tasks in the current session"
      tags:
        - "dev-workflow"
        - "execution"
        - "subagents"
      path: "skills/superpowers/subagent-driven-development"
    - name: "systematic-debugging"
      version: "1.0.0"
      description: "Use when encountering any bug, test failure, or unexpected behavior, before proposing fixes"
      tags:
        - "debugging"
        - "dev-workflow"
        - "troubleshooting"
      path: "skills/superpowers/systematic-debugging"
    - name: "test-driven-development"
      version: "1.0.0"
      description: "Use when implementing any feature or bugfix, before writing implementation code"
      tags:
        - "dev-workflow"
        - "tdd"
        - "testing"
      path: "skills/superpowers/test-driven-development"
    - name: "using-git-worktrees"
      version: "1.0.0"
      description: "Use when starting feature work that needs isolation from current workspace or before executing implementation plans - ensures an isolated workspace exists via native tools or git worktree fallback"
      tags:
        - "git"
        - "isolation"
        - "worktree"
      path: "skills/superpowers/using-git-worktrees"
    - name: "using-superpowers"
      version: "1.0.0"
      description: "Use when starting any conversation - establishes how to find and use skills, requiring skill invocation before ANY response including clarifying questions"
      tags:
        - "meta"
        - "skills"
        - "workflow"
      path: "skills/superpowers/using-superpowers"
    - name: "verification-before-completion"
      version: "1.0.0"
      description: "Use when about to claim work is complete, fixed, or passing, before committing or creating PRs - requires running verification commands and confirming output before making any success claims; evidence before assertions always"
      tags:
        - "dev-workflow"
        - "quality"
        - "verification"
      path: "skills/superpowers/verification-before-completion"
    - name: "writing-plans"
      version: "1.0.0"
      description: "Use when you have a spec or requirements for a multi-step task, before touching code"
      tags:
        - "dev-workflow"
        - "docs"
        - "planning"
      path: "skills/superpowers/writing-plans"
    - name: "writing-skills"
      version: "1.0.0"
      description: "Use when creating new skills, editing existing skills, or verifying skills work before deployment"
      tags:
        - "authoring"
        - "meta"
        - "skills"
      path: "skills/superpowers/writing-skills"
