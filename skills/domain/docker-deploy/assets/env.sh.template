#!/bin/bash
# Image build configuration - edit this file to update version
# Generated: {timestamp}

PROGRAM={registry}/{team}/{app-name}  # Full image name, including registry address
VERSION={x.y.z}                        # Semantic version
ENV=prod                               # Runtime env: prod / dev / staging
GOPROXY=https://goproxy.cn,https://goproxy.io,direct  # Go module proxy (required for China builds)
APT_MIRROR=mirrors.aliyun.com          # apt mirror for China (optional: mirrors.tuna.tsinghua.edu.cn)
