# {App Name} 部署说明

生成时间：{timestamp}

## 快速启动

```bash
# 构建镜像
./docker/build.sh

# 启动服务
cd docker && docker-compose up -d

# 查看状态
docker-compose ps
docker-compose logs -f
```

## 配置说明

| 配置项   | 默认值     | 说明           |
| -------- | ---------- | -------------- |
| 端口     | {port}     | 对外暴露端口   |
| 数据目录 | /app/data  | 持久化数据     |
| 日志目录 | /data/logs | 宿主机日志路径 |

## 注意事项

- 挂载路径需根据宿主机实际情况修改 `docker-compose.yml` 中的 volumes
- 敏感配置通过环境变量注入，不存入代码仓库
